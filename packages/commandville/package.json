{
  "name": "commandville",
  "version": "0.0.5",
  "description": "'CLI for running CLIs'",
  "type": "module",
  "exports": "./dist/index.js",
  "types": "dist/types/index.d.ts",
  "bin": {
    "cmv": "./dist/index.js",
    "commandville": "./dist/index.js"
  },
  "engines": {
    "node": "^12.20.0 || ^14.13.1 || >=16.0.0"
  },
  "files": [
    "dist/",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "shx rm -rf dist",
    "typecheck": "tsc --noEmit",
    "build:types": "tsc --emitDeclarationOnly",
    "build:package": "esbuild src/**/*.ts --sourcemap --format=esm --platform=node --target=node14 --outdir=dist",
    "build:tsc": "tsc",
    "build": "run-p build:types build:package",
    "watch:types": "pnpm build:types -- --watch",
    "watch:package": "pnpm build:package -- --watch",
    "start": "run-p watch:types watch:package",
    "release:package": "npm-publish --token $NPM_TOKEN ./package.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dworthen/commandville.git"
  },
  "keywords": [
    "CLI",
    "runner"
  ],
  "author": "Derek Worthen <worthend.derek@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dworthen/commandville/issues"
  },
  "homepage": "https://github.com/dworthen/commandville#readme",
  "dependencies": {
    "@commandville/core": "^0.1.2"
  },
  "devDependencies": {
    "@jsdevtools/npm-publish": "^1.4.3",
    "@types/glob": "^7.1.3",
    "@types/multipipe": "^3.0.0",
    "@types/node": "^15.12.4",
    "esbuild": "^0.11.17",
    "npm-run-all": "^4.1.5",
    "shx": "^0.3.3",
    "typescript": "^4.2.4"
  }
}
